import 'package:flutter/material.dart';

class AuthService {
  // register with email or phonenumber and password
  Future registerWithEmailOrPhonenumberAndPassword(
      String emailOrPhonenumber, String password) async {
    try {} catch (e) {}
  }
}
